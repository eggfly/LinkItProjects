This example demonstrates how to receive an SMS and how to retrieve the content and the sender's phone number of an SMS.

It reads an SMS message using the vm_gsm_sms_read_message() and analyses the SMS information inside the sms_read_sms_sample_callback(). Check the Monitor tool for logs of the SMS content.

This example requires a SIM card to be installed and an SMS is sent to the board.